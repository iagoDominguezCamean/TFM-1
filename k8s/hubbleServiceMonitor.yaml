apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cilium-agent
  namespace: kube-system
  labels:
    release: kube-prometheus-stack  # <-- must match your Prometheus Helm release name
spec:
  selector:
    matchLabels:
      k8s-app: cilium  # or use actual label on Hubble metrics service
  namespaceSelector:
    matchNames:
      - kube-system
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s